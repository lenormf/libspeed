
libspeed

This library implements a few boost objects, such as functors, the lexical_cast
function, or typelists. Although the tests happen to use the C++11 standard,
libspeed is supposedly fully C++03 compatible.
